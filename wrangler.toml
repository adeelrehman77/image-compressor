# Cloudflare — static site from dist/
# Build (dashboard):  npm run build
# Deploy (dashboard): npx wrangler deploy

name = "image-compressor"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[assets]
directory = "./dist"
