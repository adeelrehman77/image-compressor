/** AdSense client + unit IDs — update slots when you add units in the AdSense dashboard. */
window.__NEXUS_ADS = {
    client: 'ca-pub-7053756513182661',
    units: {
        /** Main banner below the tool workspace (all studio tabs). */
        primary: '4769485252',
        /**
         * Optional second unit on the homepage (between SEO intro and FAQ).
         * Create a new display ad unit in AdSense, paste its ID here, then redeploy.
         * Leave empty or equal to primary to keep a single unit on the homepage.
         */
        inContent: '',
    },
};
