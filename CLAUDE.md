# NexusCompress — Project Memory for Claude Code

> This file tells Claude Code everything about this project so it never needs
> to "read the entire codebase" at the start of a task. Update this file
> whenever the architecture changes significantly.

---

## Project Overview

**NexusCompress** — Privacy-first browser-based image tool suite.
- Live URL: https://compress.funadventure.ae/
- Arabic version: https://compress.funadventure.ae/ar/
- GitHub: https://github.com/adeelrehman77/image-compressor
- Current version: check `package.json` (auto-synced on build)
- Stack: Vanilla JS, PostCSS, Cloudflare Workers, Wrangler
- Deployment: `git push` to `main` → Cloudflare builds `dist/` then runs `npx wrangler deploy` (assets from `wrangler.toml`). Prefer git-push; use local `npm run deploy` only for emergency hotfixes after `npm run predeploy`.

---

## Architecture — Read This First

### Build Pipeline
```
npm run build
  → scripts/build.js          (main build orchestrator)
  → scripts/generate-ar-index.js  (generates public/ar/index.html from public/index.html)
  → scripts/verify-dist.js    (fails build if dist is stale or Arabic H1 is English)
  → scripts/version.js        (syncs version across all files)
  → dist/                     (output — what Cloudflare serves)
```

**NEVER edit `public/ar/index.html` directly** — it is generated by `generate-ar-index.js` from `public/index.html`. All changes to Arabic page must go through:
1. `public/index.html` (structure)
2. `public/js/i18n.js` (translations)
3. `scripts/generate-ar-index.js` (generation logic)

### Deployment
```
Edit source files → npm run build → git commit → git push → Cloudflare: npm run build → npx wrangler deploy
```
`wrangler.toml` serves `./dist` via `[assets]`. See `DEPLOY.md` for dashboard build/deploy command settings.
The `predeploy` script in `package.json` runs `verify-dist.js` first — build fails if checks don't pass.
Do not invent a `pages:deploy` script — this project uses Workers static assets, not the old Pages-only flow.

### Testing
`npm test` runs `SKIP_VERSION_BUMP=1 npm run build && node test.js && node scripts/qa-audit.js`. As of 2026-08-16, `scripts/qa-audit.js` (Puppeteer) exercises all 13 tool tabs, not a subset — extend that same `tabs` array when a new tool is added.

### CSS Layers (3 files, load in this order)
1. `public/css/app.css` — compiled PostCSS bundle, main design system
   - Defines: `--bg`, `--surface`, `--surface-border`, `--text`, `--text-muted`, `--accent`, `--success`, `--error`, `--warn`
   - Dark mode via `html.dark` class
2. `public/css/tokens.css` — named scale tokens on top of app.css
   - Defines: `--radius-*`, `--text-*`, `--space-*`, `--color-*`, `--btn-*`, `--card-*`
   - Has `html.dark` overrides for semantic colours
3. `public/css/nexus-extras.css` — tool-specific CSS, uses tokens from above
   - Add all new tool CSS here, use `var(--token-name)` not hardcoded values

**`public/css/styles.css`** — separate design system for guide/docs pages only. NOT loaded by main app.

### JavaScript Architecture
- All tool JS is in `public/js/tools/` as self-contained IIFEs
- Shared utilities: `window.NexusTools` (toast, downloadBlob, formatBytes, bindDropZone, etc.)
- i18n: `window.__NEXUS_TF(key, vars)` for translations, `window.__NEXUS_I18N` for raw strings
- Tool routing: `window.__NEXUS_NAVIGATE_TOOL(toolId)` to switch tabs
- Compression: `window.__NEXUS_APPLY_COMPRESSION_VALUES(preset)`, `window.__NEXUS_COMPRESS_ADD_FILES(files)`
- Goal picker: `public/js/goal-picker.js` — checks `nexus-onboarded` localStorage key

---

## Tools — All 13 Tabs

| Tab ID | Label EN | Label AR | File |
|--------|----------|----------|------|
| `compress` | Image Compressor | ضاغط الصور | `app.js` |
| `images-to-pdf` | Images to PDF | صور إلى PDF | `tools/images-to-pdf.js` |
| `pdf-suite` | PDF Merge | دمج PDF | `tools/pdf-suite.js` |
| `svg` | SVG Optimizer | تحسين SVG | `tools/svg-optimizer.js` |
| `passport-studio` | ID Photo Studio | استوديو صور الهوية | `tools/passport-studio.js` |
| `photo-checker` | Photo Checker | فاحص الصور | `tools/photo-checker.js` |
| `redactor` | Redactor | تعتيم المستندات | `tools/redactor.js` |
| `ai-upscaler` | AI Upscaler | تكبير بالذكاء الاصطناعي | `tools/ai-upscaler.js` |
| `heic-converter` | HEIC Converter | محوّل HEIC | `tools/heic-converter.js` |
| `format-converter` | Format Converter | محوّل الصيغ | `tools/format-converter.js` |
| `image-cropper` | Image Cropper | مقص الصور | `tools/image-cropper.js` |
| `collage-maker` | Collage Maker | صانع الكولاج | `tools/collage-maker.js` |
| `remove-bg` | Remove Background | إزالة الخلفية | `tools/remove-bg.js` |

### New Tool Checklist
When adding a new tool tab, update ALL of these:
- [ ] `public/index.html` — add `<a>` in `.tool-tabs` nav + `<div id="tool-panel-{id}">` panel
- [ ] `public/js/tools/{tool}.js` — new IIFE tool file
- [ ] `public/js/tools-router.js` — register tool
- [ ] `public/js/tool-meta.js` — add metadata
- [ ] `public/js/i18n.js` — add EN + AR keys for tab label, short label, headings, all UI strings
- [ ] `public/css/nexus-extras.css` — add tool-specific CSS
- [ ] `scripts/tool-routes.js` — add slug mapping
- [ ] `scripts/generate-sitemap.js` — add EN + AR tool URL entries (runs on every build)
- [ ] `scripts/generate-ar-index.js` — update verify checks if needed

---

## i18n System

**File:** `public/js/i18n.js` (~1500 lines)
**Pattern:** `window.__NEXUS_I18N = { en: { key: 'value' }, ar: { key: 'قيمة' } }`
**Usage in HTML:** `data-i18n="keyName"` on any element
**Usage in JS:** `window.__NEXUS_TF('keyName', { var: value }, 'fallback')`

**Rule:** Every new UI string MUST have both `en` and `ar` keys. Never hardcode English text in HTML that should be translatable.

**Arabic generation:** `scripts/generate-ar-index.js` applies all `data-i18n` keys to the Arabic page automatically. Build will throw if English hero text leaks into Arabic output.

---

## Design System — Key Rules

### Always Use Tokens
```css
/* CORRECT */
border-radius: var(--radius-lg, 12px);
font-size: var(--text-sm, 0.75rem);
color: var(--color-success, #22c55e);

/* WRONG — hardcoded values */
border-radius: 12px;
font-size: 0.75rem;
color: #22c55e;
```

### CSS Variable Reference
```
--bg              page background
--surface         card/panel background
--surface-border  borders (alias: --border)
--text            primary text
--text-muted      secondary text (alias: --muted)
--accent          brand green (#9aa8e8 dark / #4a5fc1 light)
--success         success green
--error           error red
--warn            warning amber

--radius-xs/sm/md/lg/xl/pill   border radius scale
--text-xs/sm/ui/base/md/lg/xl/2xl/3xl   font size scale
--space-1 through --space-12   spacing scale
--color-success/error/warn/info/ai   semantic colours (dark mode aware)
```

### Button Classes
- `.btn-primary` — green pill, main CTA (colour: `--accent`, text: `#000` dark / `#fff` light)
- `.btn-secondary` — outlined pill, secondary actions
- `.btn-ghost` — no border, icon buttons and subtle actions
- `.btn-danger` — red outlined, destructive actions (clear, delete)
- `.btn-print` — subtle, print actions

### Layout Classes
- `.tool-layout` — 2-col grid (1fr + 300px sidebar)
- `.tool-layout-wide` — 2-col grid (1fr + 340px sidebar)
- `.tool-layout-single` — single column, max 680px
- Collapses to 1 column at 760px breakpoint

### Drop Zone Structure
```html
<div class="drop-zone" id="{tool}-drop">
  <div class="drop-zone-icon">📁</div>
  <p class="drop-zone-title" data-i18n="...">Drop files here</p>
  <p class="drop-zone-subtitle" data-i18n="...">Subtitle text</p>
  <div class="drop-zone-formats">
    <span class="drop-zone-format-tag">JPEG</span>
  </div>
  <p class="drop-zone-paste-hint" data-i18n="dropPasteHint">or paste from clipboard</p>
  <button class="btn-primary">Choose files</button>
</div>
```

### Settings Card Structure (BEM)
```html
<div class="settings-card">
  <div class="settings-card__header" aria-expanded="true">
    <div class="settings-card__header-left">
      <span class="settings-card__icon">⚙️</span>
      <span data-i18n="cardTitle">Card Title</span>
    </div>
    <button class="settings-card__toggle btn-ghost">▲</button>
  </div>
  <div class="settings-card__body">
    <!-- settings fields here -->
  </div>
</div>
```

### Privacy Banner (for sensitive tools)
```html
<div class="redactor-privacy-banner chip chip-success" role="status">
  <svg><!-- lock icon --></svg>
  <span data-i18n="toolPrivacyBanner">🔒 Your files never leave this device</span>
</div>
```

---

## RTL / Arabic Rules

- Arabic page uses `<html lang="ar" dir="rtl">`
- All layout must work RTL — use `margin-inline-start` not `margin-left`
- `app.css` has RTL overrides for common patterns
- `nexus-extras.css` has `[dir=rtl]` overrides for drop-zone, tool-tabs, etc.
- Test every new CSS change in RTL — sidebar should flip to left, text aligns right

---

## Compression Presets (Quick Reference)

Defined in `app.js` as `COMPRESSOR_QUICK`:
- `emirates-id` — JPEG, target 200 KB
- `mohre` — JPEG, target 500 KB
- `whatsapp` — JPEG 80%, max 1600px
- `web` — WebP 82%, max 1920px
- `email` — JPEG 80%, max 1200px

---

## Sitemap

**File:** `public/sitemap.xml`
All guide pages + homepage + `/ar/` + 12 tool URLs under `/tools/{slug}/` + legal/docs pages = 57 sitemap URLs (extensionless; compress stays on `/`).

### Tool URL pattern

Each non-compress tool has a dedicated crawlable entry point generated at build time:

| Tool ID | EN URL | AR URL |
|---------|--------|--------|
| `images-to-pdf` | `/tools/images-to-pdf/` | `/ar/tools/images-to-pdf/` |
| `pdf-suite` | `/tools/pdf-merge/` | `/ar/tools/pdf-merge/` |
| `svg` | `/tools/svg-optimizer/` | `/ar/tools/svg-optimizer/` |
| `passport-studio` | `/tools/passport-photo/` | `/ar/tools/passport-photo/` |
| `photo-checker` | `/tools/photo-checker/` | `/ar/tools/photo-checker/` |
| `redactor` | `/tools/document-redactor/` | `/ar/tools/document-redactor/` |
| `ai-upscaler` | `/tools/ai-upscaler/` | `/ar/tools/ai-upscaler/` |
| `heic-converter` | `/tools/heic-converter/` | `/ar/tools/heic-converter/` |
| `format-converter` | `/tools/format-converter/` | `/ar/tools/format-converter/` |
| `image-cropper` | `/tools/image-cropper/` | `/ar/tools/image-cropper/` |
| `collage-maker` | `/tools/collage-maker/` | `/ar/tools/collage-maker/` |
| `remove-bg` | `/tools/remove-background/` | `/ar/tools/remove-background/` |

- **Source of truth:** `scripts/tool-routes.js` (Node) + `public/js/tool-routes.js` (browser)
- **Generated at build:** `scripts/generate-tool-pages.js` → `dist/tools/*/index.html` and `dist/ar/tools/*/index.html`
- Each page reuses the full app bundle with tool-specific `<title>`, meta description, canonical, and `window.__NEXUS_INITIAL_TOOL`
When adding new tools or guides, add entries with:
- `lastmod`: today's date
- `changefreq`: `monthly`
- `priority`: `0.8` for tools, `0.75` for guides

---

## Print Feature
- Print frame: `<div id="print-frame">` at end of body — hidden normally, shown during `window.print()` via `@media print`
- Single print: `.btn-print.print-btn` on each result card in `app.js` — calls `printSingle(task)`
- Contact sheet: `#print-contact-btn` in batch toolbar — shown when 2+ files done, calls `printContactSheet()`
- Photo sheet: inside `#passport-sidebar-export` in `index.html` — layout selector `#passport-print-layout`, crop marks `#passport-crop-marks`, print button `#passport-print-window` — handled in `passport-studio.js::printPhotoSheet()`
- Print CSS: `@media print` block + `.btn-print` in `nexus-extras.css`

## UX Polish
- Savings badge: `.result-savings-badge` on result cards in `app.js` — prominent `−{n}%` shown on completion; `is-minimal` class when < 5%
- Empty states: `.empty-state-friendly` used in `#compress-preview-empty`, `#pc-results-empty`. The redundant `#rd-empty-state` (Redactor) and `#up-empty-state` (AI Upscaler) were removed 2026-08-16 — those tools rely on their drop zone alone.
- AI loading: `.ai-loading-state` with 3 animated steps (`pc-step-1/2/3`, `up-step-1/2/3`) in `photo-checker.js` and `ai-upscaler.js`; advances via `setLoadStep()` / `setUpLoadStep()`
- Mobile bar: `#mobile-action-bar` sticky bottom — visible < 760px only, synced in `syncWorkflowUI()` inside `app.js`
- Friendly errors: `getFriendlyError(err)` helper in `app.js` — uses i18n `errGeneric/errTooLarge/errBadFile/errMemory/errNetwork`
- PDF Merge list (`pdf-suite.js::renderMergeList()`) has `↑`/`↓` reorder buttons (`data-up`/`data-down`) alongside `Remove` (`data-rm`), matching the `pdfMergeReorderHint` copy.
- Collage Maker: switching layout with photos loaded shows a `window.confirm()` gate (i18n key `collageLayoutConfirm`) before wiping slots; `#collage-compress-btn` sends the exported collage straight to the Compressor tool.
- Images to PDF exposes `window.__NEXUS_ITP_ADD_FILES` so `clipboard-paste.js` can route a pasted image directly into that tool's file list when it's the active tab.

## HEIC Converter — CSP

- **Do not use `heic2any` from CDN** — its libheif worker calls `eval()` and is blocked by `public/_headers` CSP (no `unsafe-eval`).
- Use **self-hosted** `public/vendor/heic-to-csp.min.js` from npm `heic-to` package (`dist/csp/`, built with `DYNAMIC_EXECUTION=0`).
- Vendored via `scripts/vendor-heic-to.js` on `postinstall`; loaded with dynamic `import()` in `public/js/tools/heic-converter.js`.

### HEIC support across other tools (added 2026-08-16)
HEIC/HEIF input is now accepted (not just in the dedicated HEIC Converter) in: `image-cropper.js`, `format-converter.js`, `collage-maker.js`, `remove-bg.js`, `photo-checker.js`. Each detects HEIC via MIME type or `.heic`/`.heif` extension, then calls the shared `window.NexusTools.convertHeicToJpegFile(file)` (defined in `tools-shared.js`) before processing, showing `heicConverting` / `heicConvertFailed` toasts. Follow this same pattern for any new tool that accepts image uploads — check `tools-shared.js` for the helper rather than re-implementing conversion.

## AI Tools — Special Notes

### AI Upscaler
- ONNX model self-hosted at `public/models/realesrgan-x4.onnx` (~4.87 MB)
- Worker: `public/js/upscaler-worker.mjs`
- Model loaded lazily on tab open, cached via Cache API key `nexus-esrgan-model-v2`
- Tiled inference (128×128) in worker; main thread stitches into a Float32 buffer capped at ~4M output pixels (`MAX_ACCUM_OUTPUT_PIXELS`). Larger inputs are pre-scaled before AI, then bicubic-resized to the requested output size.

### Photo Checker (face-api.js)
- Models loaded from jsDelivr CDN lazily on tab open
- Uses TinyFaceDetector + face_landmark_68_tiny
- 10 checks: face detected, face size 55-85%, centred, eyes open, head tilt <5°, JPEG format, file size, dimensions, white background, no glasses

---

## Common Gotchas

1. **Never edit `public/ar/index.html` directly** — always regenerated by build
2. **Prefer `git push` for production** — Cloudflare runs build + `npx wrangler deploy`. Local `npm run deploy` is for emergency hotfixes only (after `predeploy`).
3. **Always run `npm run build` before committing** — verify-dist.js will catch stale files
4. **New i18n keys** — must be in BOTH `en` AND `ar` objects or build may warn
5. **`tabUpscaler` key** — do NOT include emoji prefix (🔬 is already in the HTML icon span)
6. **CSS colours** — never hardcode `#22c55e`, use `var(--color-success)` etc.
7. **`margin-left/right`** — use `margin-inline-start/end` for RTL compatibility
8. **Global transitions** — the narrowed rule in nexus-extras.css excludes buttons/inputs on purpose
9. **Service worker & version fetch paths must be absolute** (`/sw.js`, `/version.json` in `app.js`) — relative paths break on tool subpages like `/tools/passport-photo/` since they'd resolve against that subpath instead of root. Fixed 2026-08-16.

---

## Guides Pages

13 SEO guides in `public/guides/` — all static HTML files with:
- Matching NexusCompress design (loads `styles.css` — different from main app)
- FAQPage JSON-LD schema
- hreflang and canonical tags
- Internal links back to tool

Arabic guide versions exist for key UAE portal guides.

---

## Google Search Console

- Sitemap submitted: `https://compress.funadventure.ae/sitemap.xml`
- Status: Success, 28 pages discovered (as of 2026-05-28)
- Property: `https://compress.funadventure.ae/`

---

## What NOT to Do

- Do NOT add inline styles to HTML — use CSS classes only
- Do NOT use `localStorage` for anything user-facing beyond preferences
- Do NOT upload files to any server — all processing must be browser-local
- Do NOT add new npm dependencies without checking bundle size impact
- Do NOT break the Arabic RTL layout when adding new CSS
- Do NOT use `window.alert()` — use `window.NexusTools.toast()` instead
- Do NOT hardcode version strings — use `scripts/version.js`
